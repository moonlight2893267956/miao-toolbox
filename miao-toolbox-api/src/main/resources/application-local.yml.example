# ============================================================
# 本地开发密钥模板 — 复制为 application-local.yml 并填入真实值
# cp application-local.yml.example application-local.yml
# ============================================================

miao:
  jwt:
    secret: your-jwt-secret-key-at-least-256-bits-long
    refresh-secret: your-jwt-refresh-secret-key-at-least-256-bits-long
  oauth:
    github:
      client-id: your-github-client-id
      client-secret: your-github-client-secret
    google:
      client-id: your-google-client-id
      client-secret: your-google-client-secret
  cos:
    secret-id: your-cos-secret-id
    secret-key: your-cos-secret-key
    bucket: your-cos-bucket-name
  ai:
    agents:
      diff-explainer:
        enabled: true
        base-url: http://localhost:8000
        api-key: your-diff-explainer-api-key
      json-repairer:
        enabled: true
        base-url: http://localhost:8000
        api-key: your-json-repairer-api-key
      translate-agent:
        enabled: true
        base-url: http://localhost:8000
        api-key: your-translate-agent-api-key
  baidu:
    translate:
      enabled: true
      app-id: your-baidu-appid
      secret: your-baidu-secret
      image-url: https://fanyi-api.baidu.com/api/trans/sdk/picture
      # 语音翻译（FR-12, story-3.1）可选配置：
      # voice-url 省略时使用代码内置默认值，无需配置。
      # secret-key 省略时自动复用上方 secret（多数账号二者相同）；
      # 仅当百度后台「语音翻译」使用独立 secretKey 时取消注释并填写：
      # voice-url: https://fanyi-api.baidu.com/api/trans/v2/voicetrans
      # secret-key: your-baidu-voice-secret-key
