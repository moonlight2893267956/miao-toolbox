name: Deploy

# 推送 main 自动部署（构建并上线 latest）；
# 手动触发(workflow_dispatch)可填 deploy_tag=sha-xxxx 直接回滚到旧版本（跳过测试与构建）
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      deploy_tag:
        description: '镜像标签。留空=部署 latest（普通发布）；填 sha-xxxx 回滚到指定版本'
        required: false
        default: ''

# 同一时间只允许一个部署流水线运行，排队而非取消（避免中断正在进行的部署）
concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  # ─────────────────────────────────────────────
  # Job 1: 测试闸门（复用 ci.yml 逻辑，确保部署前代码质量）
  # ─────────────────────────────────────────────
  test:
    name: 测试闸门
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: 运行 API 测试
        working-directory: miao-toolbox-api
        run: ./mvnw test --batch-mode

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: miao-toolbox-web/package-lock.json

      - name: 安装前端依赖
        working-directory: miao-toolbox-web
        run: npm ci

      - name: TypeScript 类型检查
        working-directory: miao-toolbox-web
        run: npm run typecheck

      - name: ESLint 检查
        working-directory: miao-toolbox-web
        run: npm run lint

      - name: 前端生产构建
        working-directory: miao-toolbox-web
        run: npm run build

  # ─────────────────────────────────────────────
  # Job 2: 构建并推送镜像到 GHCR（双标签 :latest + :sha-<短哈希>）
  # ─────────────────────────────────────────────
  build-and-push:
    name: 构建并推送镜像
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 登录 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 生成镜像元数据
        id: meta
        run: |
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          SHA_SHORT=${GITHUB_SHA::7}
          {
            echo "api_image=ghcr.io/${REPO_LOWER}-api"
            echo "web_image=ghcr.io/${REPO_LOWER}-web"
            echo "sha_short=${SHA_SHORT}"
          } >> "$GITHUB_OUTPUT"

      - name: 构建并推送 API 镜像
        uses: docker/build-push-action@v6
        with:
          context: ./miao-toolbox-api
          file: ./miao-toolbox-api/Dockerfile
          push: true
          tags: |
            ${{ steps.meta.outputs.api_image }}:latest
            ${{ steps.meta.outputs.api_image }}:sha-${{ steps.meta.outputs.sha_short }}
          cache-from: type=gha,scope=api
          cache-to: type=gha,scope=api,mode=max

      - name: 构建并推送 Web 镜像
        uses: docker/build-push-action@v6
        with:
          context: ./miao-toolbox-web
          file: ./miao-toolbox-web/Dockerfile
          push: true
          tags: |
            ${{ steps.meta.outputs.web_image }}:latest
            ${{ steps.meta.outputs.web_image }}:sha-${{ steps.meta.outputs.sha_short }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,scope=web,mode=max

  # ─────────────────────────────────────────────
  # Job 3: SSH 到 VPS 拉取镜像并重启
  # ─────────────────────────────────────────────
  deploy:
    name: 部署到生产
    # 普通发布(push)需先构建；手动回滚(dispatch)直接部署旧 tag，跳过构建
    needs: ${{ github.event_name == 'push' && fromJSON('["build-and-push"]') || fromJSON('[]') }}
    runs-on: ubuntu-latest
    environment: prod
    env:
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      # 回滚时取输入的 deploy_tag；普通发布为 latest
      IMAGE_TAG: ${{ github.event.inputs.deploy_tag || 'latest' }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 配置 SSH 密钥
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null

      - name: 传输 compose 文件和部署脚本到服务器
        run: |
          ssh -i ~/.ssh/deploy_key "$DEPLOY_USER@$DEPLOY_HOST" "mkdir -p /opt/miao-toolbox"
          scp -i ~/.ssh/deploy_key docker-compose.prod.yml "$DEPLOY_USER@$DEPLOY_HOST:/opt/miao-toolbox/"
          scp -i ~/.ssh/deploy_key scripts/deploy-remote.sh "$DEPLOY_USER@$DEPLOY_HOST:/tmp/deploy-remote.sh"

      - name: 执行远程部署
        run: |
          ssh -i ~/.ssh/deploy_key "$DEPLOY_USER@$DEPLOY_HOST" \
            "chmod +x /tmp/deploy-remote.sh && IMAGE_TAG='${{ env.IMAGE_TAG }}' GHCR_TOKEN='${{ secrets.DEPLOY_GHCR_TOKEN }}' GHCR_OWNER='${{ github.repository_owner }}' /tmp/deploy-remote.sh"

      - name: 部署失败提示
        if: failure()
        run: |
          echo "::error::部署失败!请检查上方日志"
          echo "排查步骤:"
          echo "  1. SSH 到服务器: ssh $DEPLOY_USER@$DEPLOY_HOST"
          echo "  2. 查看日志: cd /opt/miao-toolbox && docker compose -f docker-compose.prod.yml logs --tail=50"
          echo "  3. 回滚: 在 Actions → Deploy → Run workflow,deploy_tag 填旧 sha（如 sha-a1b2c3d）"
