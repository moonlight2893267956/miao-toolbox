# SECRET_GROUP / db-and-cache.yaml — dev 直接写 dev 占位值（不引用环境变量）
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/miao_toolbox?useSSL=false&serverTimezone=Asia/Shanghai"
    username: "root"
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: "localhost"
      port: 6379
      username: ""
      password: ""
